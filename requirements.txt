# ── Core ─────────────────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0
rich>=13.7.0            # pretty terminal output (CLI mode: python main.py --cli)
flask>=3.0.0           # web HUD server — the primary JARVIS interface

# ── UI automation (required for messaging + window control) ─
pyautogui>=0.9.54      # screenshots, keyboard/mouse
pygetwindow>=0.0.9     # list/focus/minimize/maximize/close windows (Windows-first)
pyperclip>=1.8.2       # clipboard paste for typing unicode text (WhatsApp messages, etc.)

# Windows-specific: extended power controls + system info
pywin32>=306 ; sys_platform == "win32"
screen-brightness-control>=0.16  # set_brightness tool (get/set/up/down)

# ── Voice (optional but recommended) ─────────────────
# STT — microphone speech recognition
SpeechRecognition>=3.10.0
# pyaudio is needed for microphone input.
#   Windows:  pip install pipwin && pipwin install pyaudio
#             (or)  pip install pyaudio
#   macOS:    brew install portaudio && pip install pyaudio
#   Linux:    sudo apt install portaudio19-dev && pip install pyaudio

# TTS — offline text-to-speech (cross-platform)
pyttsx3>=2.90

# ── Automations helpers ──────────────────────────────
psutil>=5.9.0          # system stats / process control
simpleeval>=0.10.0     # safe arithmetic (replaces eval in calculate)

# ── File creators (PDF, DOCX, XLSX, PPTX, YAML) ─────
reportlab>=4.0         # PDF generation (create_pdf)
python-docx>=1.1       # DOCX generation (create_docx)
openpyxl>=3.1          # XLSX generation (create_xlsx)
python-pptx>=0.6       # PPTX generation (create_pptx)
PyYAML>=6.0            # YAML serialization (create_yaml)

# ── LAN QR code (for phone access) ───────────────────
qrcode[pil]>=7.4       # server-side QR PNG generation

# ── Telegram bot (optional — control laptop from anywhere) ─
# pip install python-telegram-bot
python-telegram-bot>=21.3  # async-first v21, no decorator gymnastics

# ── Email (SMTP/IMAP) ─────────────────────────────────
# Built-in: smtplib, imaplib, email — no extra deps needed

# ── File type detection ───────────────────────────────
python-magic>=0.4.27    # MIME type detection for file explorer
